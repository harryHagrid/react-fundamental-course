

import React from 'react';

// The function creates a button component

function AppButton() {
    return (
        <div>
            <button className="btn btn-primary">
                My Button
            </button>
        </div>
    );
}

export default AppButton;